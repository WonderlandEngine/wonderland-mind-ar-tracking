{
  "name": "mindar-image-tracking",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild image-tracking.js --sourcemap --bundle --outfile=dist/image-tracking.js",
    "dev": "esbuild image-tracking.js --sourcemap --bundle --outfile=examples/image-tracking/js/image-tracking.js --watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.14.19"
  },
  "dependencies": {
    "gl-matrix": "^3.4.3"
  }
}
